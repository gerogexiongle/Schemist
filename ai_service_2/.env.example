# =============================================================================
# Schemist · ai_service_2 — copy to `.env` and adjust (never commit `.env`).
#
# 路径默认（不设 SCHEMA_BASE_DIRS 时）：
#   repo 根 = Schemist 目录 → `<repo>/hivebrain` 与 `<repo>/table_filter_schemas`
# 覆盖：SCHEMA_BASE_DIRS 或 SCHEMIST_WORKSPACE_DIR / SQL_AI_WORKSPACE_DIR。
# 配置读取：`app.py` load_dotenv → `config/settings.py`。
# =============================================================================

# -----------------------------------------------------------------------------
# LLM (OpenAI-compatible Chat Completions; required)
# -----------------------------------------------------------------------------
LLM_API_URL=https://api.openai.com/v1/chat/completions
LLM_API_KEY=

LLM_MODEL=gpt-4o-mini

# Optional: dropdown models JSON, e.g. gateway route ids:
# LLM_MODEL_CHOICES_JSON=[["gpt-4o-mini","GPT-4o mini"],["gpt-4o","GPT-4o"]]

# -----------------------------------------------------------------------------
# SQL 生成稳定性与成本控制：默认模型先检索 4 轮，随后禁用工具强制输出 SQL。
# 强模型只在收口失败、校验仍失败或复杂多表 JOIN 时额外调用一次；留空可关闭兜底。
# SQL_AGENT_FALLBACK_MODEL=gpt-4o
# 最终强模型完整生成允许 120 秒；执行失败后的强模型修复最多等待 60 秒
# SQL_AGENT_FINAL_FALLBACK_TIMEOUT=120
# SQL_AGENT_REPAIR_FALLBACK_TIMEOUT=60
# SQL_AGENT_FALLBACK_TIMEOUT=120
# SQL_AGENT_FALLBACK_MAX_RETRIES=0
# 已选择这些强模型时不再二次兜底；逗号分隔、统一按小写比较
# SQL_AGENT_STRONG_MODELS=gpt-4o,gpt-4-turbo
# SQL_AGENT_MAX_TOOL_ROUNDS=4
# SQL_AGENT_SEARCH_TABLES_LIMIT=2
# SQL_AGENT_GET_TABLE_INFO_LIMIT=6
# SQL_AGENT_MAX_TOTAL_TOOL_CALLS=8
# SQL_AGENT_CATALOG_TOP_N=15
# SQL_AGENT_COMPLEX_TABLE_THRESHOLD=3
# 复杂漏斗扩展（普通问题仍使用上面的默认预算）
# SQL_AGENT_COMPLEX_STAGE_TOP_N=3
# SQL_AGENT_COMPLEX_CATALOG_MAX=24
# SQL_AGENT_COMPLEX_MAX_TOOL_ROUNDS=6
# SQL_AGENT_COMPLEX_SEARCH_TABLES_LIMIT=6
# SQL_AGENT_COMPLEX_GET_TABLE_INFO_LIMIT=10
# SQL_AGENT_COMPLEX_MAX_TOTAL_TOOL_CALLS=16
# SQL_AGENT_COMPLEX_FEEDBACK_TOP_N=5

# 漏斗配置默认不加载业务映射；config/complex_query_mappings.example.json 仅提供空结构
# COMPLEX_QUERY_MAPPING_PATH=

# -----------------------------------------------------------------------------
# Local schema KB (`<database>/json/*.json`); multiple paths `;`-separated
# -----------------------------------------------------------------------------
# SCHEMA_BASE_DIRS=

# -----------------------------------------------------------------------------
# Writable workspace root (temp_sql_v2); alias: SQL_AI_WORKSPACE_DIR
# -----------------------------------------------------------------------------
# SCHEMIST_WORKSPACE_DIR=

# -----------------------------------------------------------------------------
# Trino
# -----------------------------------------------------------------------------
TRINO_HOST=localhost
TRINO_PORT=8080
TRINO_USER=schemist
TRINO_PASSWORD=
TRINO_CATALOG=hive
TRINO_SCHEMA=default
# TRINO_ROLES=hive=admin
# TRINO_HIVE_ROLE_PRIORITY=admin,analytics,public
# TRINO_SET_HIVE_ROLE_FALLBACK=1

# -----------------------------------------------------------------------------
# Spark / SQL execution
# -----------------------------------------------------------------------------
SPARK_QUEUE=default
# SPARK_DYNAMIC_ALLOCATION_ENABLED=true
# SPARK_DYNAMIC_ALLOCATION_MAX_EXECUTORS=80
# SPARK_EXTRA_CONFS=spark.speculation=true
# SPARK_SQL_SET_OVERRIDES=spark.sql.shuffle.partitions=600
# SQL_EXECUTOR_TIMEOUT=900

# -----------------------------------------------------------------------------
# Public base URL (share / Feishu); alias: SQL_AI_PUBLIC_BASE_URL
# -----------------------------------------------------------------------------
# SERVICE_PUBLIC_ORIGIN=http://127.0.0.1:8889

# -----------------------------------------------------------------------------
# Feishu (optional)
# -----------------------------------------------------------------------------
FEISHU_APP_ID=cli_xxx
FEISHU_APP_SECRET=
FEISHU_DOC_FOLDER_TOKEN=
FEISHU_DOC_BASE_URL=https://www.feishu.cn

# -----------------------------------------------------------------------------
# Optional: query limits & Skills Pack admin
# -----------------------------------------------------------------------------
# QUERY_RESULT_MAX_ROWS=2000
# SKILLS_ADMIN_TOKEN=
# PIPELINE_TRACE_UI=1
# PIPELINE_TRACE_LOG=1
# PIPELINE_TRACE_UI_TTL_SEC=3600
# SCHEMA_FEEDBACK_PATH=

# -----------------------------------------------------------------------------
# Feishu bot / card tuning (optional)
# -----------------------------------------------------------------------------
# FEISHU_REPLY_MAX_CHARS=18000
# FEISHU_CARD_MD_CHUNK=3800
# FEISHU_CARD_SCHEMA=2
# FEISHU_REPLY_INTERACTIVE=1
# FEISHU_APP_MAPPINGS=
# FEISHU_BOT_DEFAULT_PIPELINE=full
# FEISHU_AUTO_SKILL_MATCH=1
# FEISHU_AUTO_SKILL_EXCLUDE_IDS=data-insights
# FEISHU_AUTO_SKILL_MIN_SCORE=65
# FEISHU_AUTO_SKILL_MARGIN=12

# MCP：默认开启；设置 Token 后客户端需携带 Bearer Token
# MCP_ENABLED=1
# MCP_API_TOKEN=

# PageAgent 通过服务端代理使用 LLM，浏览器只接收占位 key
# PAGE_AGENT_MODEL=gpt-4o-mini
